# PROJECT — сайт 3d-scan-nn.ru

Сайт услуг лазерного 3D-сканирования и обмеров (Нижний Новгород),
ИП Фролов Роман Александрович. Цель — заявки на сканирование/обмеры.
Статический сайт на GitHub Pages, домен `3d-scan-nn.ru`.

- **Репозиторий:** `danielharris-openclaw/3d-scan-nn` (GitHub Pages из ветки `main`)
- **Домен:** https://3d-scan-nn.ru/ (`CNAME`, HTTPS)
- **Канон знаний:** вольт `second-brain`, заметка `wiki/3d-scan-nn-sayt.md`

## Рабочие документы

- [AGENTS.md](AGENTS.md) — правила работы в этом репозитории.
- [PROJECT_STATUS.md](PROJECT_STATUS.md) — актуальное состояние и следующий этап.
- [План поискового продвижения и роста обращений](docs/superpowers/plans/2026-09-22-seo-growth.md).
- [Исходная проверка от 22.09.2026](docs/SEO_BASELINE.md) — наблюдения, неизвестные показатели и ограничения проверки.

Этот файл описывает устройство проекта и его историю. Прежние сведения о доставке формы,
лимитах сервисов и результатах проверок не являются подтверждением их текущего состояния.
Ссылка на `second-brain` не даёт разрешения открывать или обновлять внешний вольт.

## Что это и откуда взялось

На входе был **дизайн-эталон из Claude Design** — «bundled» HTML (`3D-Scan NN (готовый
файл).html`, ~4 МБ): реальная вёрстка лежала JSON-строкой в `<script type="__bundler/template">`,
а картинки (16 шт.) и шрифты (24 woff2) — base64 в манифесте. Это был визуальный образец,
а не рабочий сайт: интерактив держался на проприетарном рантайме Claude Design (`<x-dc>`,
`DCLogic`, `sc-for`, `{{ }}`-биндинги, JS-бандл).

До этого в репозитории жил **другой, старый дизайн** (бежево-зелёная «бумажная» тема, Onest).
Его заменили новым графитовым прототипом 1:1, сохранив инфраструктуру публикации.

## Архитектура (ключевое решение)

Прототип главной перенесён в **чистую статику: HTML + CSS + vanilla JS, без рантайма Claude Design**.
На главной сохранены инлайн-стили; калькулятор и FAQ работают на vanilla JS.
Страница помещений — отдельный HTML со своими стилями и нативным FAQ `details`/`summary`.
Её кнопки ведут к существующей форме главной `/#leadform`; форма и калькулятор не дублируются.
Сборки нет, GitHub Pages отдаёт статику напрямую.

Дизайн-токены: фон `#0E1219`/`#14181F`, синий `#0B5FFF`, оранжевый CTA `#FF5A1F`;
шрифты Space Grotesk / Manrope / IBM Plex Mono (self-hosted woff2).

## Структура

```
index.html            — главная (вёрстка + CSS в <head> + vanilla JS внизу)
uslugi/obmery-pomeshcheniy/
  index.html          — обмеры квартир и помещений, учебный пример состава выдачи
privacy.html          — политика конфиденциальности (152-ФЗ)
robots.txt, sitemap.xml
assets/img/*.webp      — 16 изображений (вынесены из base64, сжаты в WebP)
assets/fonts/*.woff2   — 24 шрифта (self-host)
CNAME, favicon.svg/png, apple-touch-icon.png
```

## Калькулятор (Вариант B — детальный)

Перенесён **из прежней версии сайта** (старый `index.html`) со всеми реальными ставками,
визуально пересобран в графитовом стиле. 5 вкладок, расчёт обновляется на лету:
- **Помещения:** ставки 60–260 ₽/м² по типу результата (скан ч/б/цвет, план, +инж.сети,
  3D, Archicad), минимумы независимо от площади: 20 000 ₽, модель с сетями — 30 000 ₽,
  Archicad с сетями — 35 000 ₽; LOD200/300 (×1.5), НДС 0/5/25 %, км×200 ₽,
  скидки за объём 1000+ м² (10/25/50 %), ОКН ×1.35, срочность +20 %.
- **Фасады:** ставка = скан+чертёж+модель ₽/м² (+ортофото 30), мин. 50 000 ₽, автовышка
  +30 000, ОКН ×1.35.
- **Топо/ВЛС:** 20/25/35 тыс ₽/га; ВЛС мин. 300 000 ₽.
- **Резервуары:** база по объёму (30–60 тыс ₽/шт) + документы, скидка 10+ шт.
- **Реверс:** база × коэффициент точности (1…4) + CAD.
Кнопки «Скопировать текст для КП» и «Отправить расчёт в заявку» (подставляет расчёт в форму).
Общий нижний предел итогового расчёта во всех вкладках — 20 000 ₽; более высокие минимумы
отдельных услуг сохраняются. Правило подтверждено владельцем 23.09.2026.

Локальные проверки: `node --test tests/calculator.test.cjs tests/contact-events.test.cjs tests/lead-form.test.cjs tests/interior-contact-events.test.cjs`. Тесты выполняют реальные
встроенные скрипты главной и страницы помещений с локальной имитацией элементов страницы, проверяют
границы площадей, минимумы, действующие скидки, надбавки, перенос суммы в форму,
события контактных ссылок и состояния отправки формы без файлов.
Сеть и отправка реальных заявок в этой проверке не используются. Состояние публикации — в `PROJECT_STATUS.md`.

## Контакты (боевые)

- Телефон: **+7 903 606-00-54** (`tel:+79036060054`)
- Email: **frolovr9@gmail.com** (`mailto:`)
- Telegram: **https://t.me/frolovr9**
- MAX: ссылка профиля предоставлена владельцем и установлена в `index.html`; используется для кнопок связи и отправки материалов.
- Реквизиты в футере: ИП Фролов Роман Александрович · ИНН 525903804858

## Интеграции

- **Форма заявки → Formspree** `https://formspree.io/f/mlgybbgv` → письма на frolovr9@gmail.com.
  Поля: имя*, телефон* (маска/валидация), адрес/площадь, тип объекта, желаемый результат,
  комментарий, скрытое поле расчёта, honeypot,
  согласие на ПДн*. Отправка через fetch (Accept: application/json), inline-сообщения
  успех/ошибка. Поля загрузки файлов нет: материалы предлагается отправлять через MAX,
  Telegram или почту. Текущий результат проверки доставки и антиспама — в `PROJECT_STATUS.md`.
  *На проверенном тарифе Free — 50 отправок/мес., загрузка файлов не поддерживается.*
- **Яндекс.Метрика** счётчик **110175409** (webvisor, clickmap). Цели через `ym(...,'reachGoal')`:
  `lead` (успешный ответ Formspree, не доказательство доставки письма), `call`, `telegram`,
  `max`, `email`, `calc`. ID созданных целей и ограничения проверки — в `PROJECT_STATUS.md`.

> Эти ID — клиентские (видны в HTML), не секреты. Токенов/паролей в репозитории нет.

## SEO / юр / адаптив

- `title`/`description`, Open Graph, canonical, schema.org `LocalBusiness` (ИНН, телефон,
  гео НН, услуги), `robots.txt`, `sitemap.xml`, один H1, фавиконки.
- `privacy.html` (152-ФЗ) + cookie-уведомление (localStorage).
- Адаптив: медиа-запросы через атрибут-селекторы `[style*="..."]` с `!important` —
  инлайн-гриды прототипа схлопываются на мобильных без правки разметки (десктоп 1:1).
- Картинки: base64 → WebP (**2.6 МБ → 0.97 МБ**, −63 %), `loading=lazy/eager`, осмысленные `alt`.
- Проверено Playwright: десктоп 1:1, мобильный без горизонтального скролла, консоль чистая.

## Публикация (важно)

Пуш в репозитории аккаунта `danielharris-openclaw` с этого ПК идёт **только по SSH**
(HTTPS-токен GCM read-only → 403). Remote переведён на SSH:
`git@github.com:danielharris-openclaw/3d-scan-nn.git`. См. вольт `wiki/github-push-s-pk.md`.
После пуша в `main` GitHub Pages пересобирает сайт за ~1–2 мин.

## История

- **2026-06-26:** перенос прототипа на статику (вёрстка 1:1, калькулятор Вариант B и FAQ
  на vanilla JS, картинки WebP, шрифты self-host), боевые контакты, SEO, форма (Formspree),
  политика, robots/sitemap, Метрика+цели, cookie, адаптив. Подключены боевые Formspree
  (`mlgybbgv`) и Метрика (`110175409`). Опубликовано на https://3d-scan-nn.ru/.

## Исторический бэклог

Текущие приоритеты и состояние задач ведутся в `PROJECT_STATUS.md` и плане продвижения.
Список ниже сохранён как исходный контекст.

- Финальные фото кейсов (сейчас стоят из прототипа) — заменить на реальные, проставить `alt`.
- Согласовать/перепроверить тарифы калькулятора при изменении прайса.
- Прогнать Lighthouse (производительность/доступность) и доработать по отчёту.
- Опционально: вынести CSS/JS из `index.html` в отдельные файлы, де-инлайнить стили.
