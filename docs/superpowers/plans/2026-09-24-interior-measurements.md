# Страница «Обмеры квартир и помещений» — план локальной реализации

> Для исполнения применять `superpowers:executing-plans`, выполняя пункты последовательно. Работать самостоятельно в текущем репозитории; отдельный clone/worktree и параллельные агенты не согласованы.

**Цель:** подготовить локальную страницу `/uslugi/obmery-pomeshcheniy/` по [согласованному проекту](../specs/2026-09-24-interior-measurements-design.md), связать её с главной и проверить обращение через существующую форму.

**Архитектура:** один новый статический HTML-файл со своими стилями и минимальным JavaScript. Главная получает обычную ссылку, `sitemap.xml` — один URL. Форма и калькулятор остаются на главной; их обработчики не переносятся в новую страницу.

**Технологии:** HTML, CSS, vanilla JavaScript, существующие локальные шрифты; встроенный `node:test` для текущих проверок. Сборки, новых зависимостей и нового движка нет.

**Основание и разрешение:** исходная запись `48954a0`; 24.09.2026 владелец согласовал URL, восемь блоков, типовой сценарий и переход `/#leadform` без автоматического заполнения полей. Последующими командами «делай» и «закрой задачу» разрешил локальную реализацию, фиксацию в Git и штатную публикацию. Локальный план выполнен; результаты проверок сохранены в [PROJECT_STATUS.md](../../../PROJECT_STATUS.md).

## 1. Карта файлов

| Файл | Действие и назначение |
|---|---|
| `uslugi/obmery-pomeshcheniy/index.html` | Создать страницу: восемь блоков, метаданные, стили, контакты и учёт их кликов |
| `index.html` | Добавить одну видимую ссылку в разделе «Типовые объекты, которые мы сканируем» |
| `sitemap.xml` | Добавить один URL новой страницы |
| `tests/interior-contact-events.test.cjs` | Проверить реальные обработчики новой страницы: события по каналам, отсутствие `lead` при навигации, отказ счётчика |
| `PROJECT_STATUS.md` | После выполнения описать фактический результат и проведённые проверки |

`assets/`, `privacy.html`, тарифы, обработчики главной, служебные файлы подтверждения прав и настройки кабинетов сохраняются. Исторические прототипы и фотографии неподтверждённых кейсов для новой страницы не используются.

## 2. Проверить исходное состояние

- [x] Прочитать `AGENTS.md`, актуальный `PROJECT_STATUS.md` и согласованный проект. Проверить Git и относящийся к задаче diff:

```powershell
git status --short --branch
git log -1 --oneline
git diff -- index.html sitemap.xml PROJECT_STATUS.md
```

Ожидается понятный состав исходных изменений. Локальные документы согласования могут быть ещё не зафиксированы; не удалять их. При чужих пересекающихся изменениях остановить только затронутую работу.

- [x] Выполнить существующие проверки главной:

```powershell
node --test tests/calculator.test.cjs tests/contact-events.test.cjs tests/lead-form.test.cjs
```

На исходной версии предусмотрено 20 проверок. До правок зафиксировать фактический результат; тесты используют подмену отправки и счётчика.

- [x] Сверить `minInterior`, `interiorRate`, `calcInterior` в `index.html` с четырьмя строками цен проекта: 60/80/80/120 ₽ за м², минимум 20 000 ₽. Базовый план 100 м² — 20 000 ₽, 350 м² — 28 000 ₽; повышенные минимумы моделирования 30 000/35 000 ₽ сохраняются. При расхождении тарифов выяснить причину до переноса текста.

## 3. Создать страницу

**Файл:** `uslugi/obmery-pomeshcheniy/index.html`.

- [x] Создать HTML с `lang="ru"`, UTF-8 и viewport. Задать точные метаданные:

```html
<title>Обмеры квартир и помещений в Нижнем Новгороде — 3D-Scan NN</title>
<meta name="description" content="Лазерные обмеры квартир и коммерческих помещений в Нижнем Новгороде и области. Облако точек, планы DWG/PDF. Минимальный заказ — 20 000 ₽.">
<link rel="canonical" href="https://3d-scan-nn.ru/uslugi/obmery-pomeshcheniy/">
<meta property="og:type" content="website">
<meta property="og:title" content="Обмеры квартир и помещений в Нижнем Новгороде — 3D-Scan NN">
<meta property="og:description" content="Лазерные обмеры квартир и коммерческих помещений в Нижнем Новгороде и области. Облако точек, планы DWG/PDF. Минимальный заказ — 20 000 ₽.">
<meta property="og:url" content="https://3d-scan-nn.ru/uslugi/obmery-pomeshcheniy/">
<meta property="og:locale" content="ru_RU">
```

- [x] Применить `frontend-design` для вёрстки в существующем оформлении: Manrope, Space Grotesk и IBM Plex Mono, графитовый фон, синий и оранжевый акценты из главной. Объявления нужных шрифтов брать из текущего `index.html`; пути к ресурсам новой страницы начинать с `/assets/`, к иконкам — с `/`. Не выносить общие стили главной и не менять её оформление.

- [x] Перенести публичные тексты из раздела 4 согласованного проекта полностью, сохранив порядок. Строки «Примечание к реализации» в публичный HTML не переносить:

| Блок | Содержимое и условие проверки |
|---|---|
| Первый экран | Единственный H1 «Обмеры квартир и помещений в Нижнем Новгороде», описание, минимум 20 000 ₽, две ссылки |
| Задачи | Квартира, офис/магазин, сложная геометрия |
| Результат | Облако точек, план DWG/PDF, план с видимыми сетями; отдельное согласование 3D-модели |
| Стоимость, `id="prices"` | Четыре варианта цен, пояснения к минимуму, оба базовых примера, НДС и дополнительные опции |
| Типовой сценарий | «Квартира перед разработкой дизайн-проекта», явное пояснение «Пример задачи», без вымышленных параметров заказа |
| Этапы | Описание задачи → согласование → сканирование → передача материалов |
| FAQ | Семь вопросов и ответов из проекта; для раскрытия использовать нативные `details`/`summary` |
| Обращение | «Опишите помещение — подберём состав обмеров», кнопка формы и четыре прямых контакта |

- [x] Основные кнопки первого, ценового и финального блоков направить к форме на главной. Дополнительную ссылку первого экрана — к ценам. Варианты разметки ссылок:

```html
<a href="/#leadform">Получить расчёт обмеров</a>
<a href="#prices">Посмотреть состав и цены</a>
<a href="/#leadform">Получить расчёт для своего помещения</a>
<a href="/">На главную</a>
<a href="/privacy.html">Политика конфиденциальности</a>
```

Не добавлять к переходам `data-goal="lead"`, параметры заполнения полей или отправку данных. Общие контакты и реквизиты брать из действующей главной. Полный адрес из Яндекс.Бизнеса не переносить.

- [x] Обеспечить последовательную раскладку на узких экранах: полные названия вариантов, читаемые цены, перенос строк, видимый фокус ссылок, доступные кнопки. Минимум заказа и пометка типового сценария должны оставаться заметными.

## 4. Сохранить учёт контактов и уведомление cookie

**Файл:** `uslugi/obmery-pomeshcheniy/index.html`.

- [x] Для телефона, Telegram, MAX и почты скопировать точные `href` из главной. Соответствующие `data-goal`: `call`, `telegram`, `max`, `email`. Не придумывать адрес MAX и не добавлять обработчики `onclick`.

- [x] Скопировать действующий блок счётчика `110175409` между комментариями `Yandex.Metrika counter` и `/Yandex.Metrika counter`; сохранить идентификатор и параметры. Для контактных ссылок применить существующий обработчик один раз:

```html
<script>
(function(){
  function ymGoal(g){try{if(window.ym&&window.METRIKA_ID&&window.METRIKA_ID!=="XXXXXXXX")ym(window.METRIKA_ID,"reachGoal",g);}catch(e){}}
  document.querySelectorAll('[data-goal]').forEach(function(a){a.addEventListener('click',function(){ymGoal(a.dataset.goal);});});
})();
</script>
```

- [x] Перенести блок `#cookie`, его стили и существующий обработчик `cookieOk` из главной. На новой странице ссылка политики — `/privacy.html`; ключ `localStorage` остаётся `cookieOk`. Не переносить обработчики формы, маски телефона и калькулятора.

## 5. Связать страницу с главной и картой сайта

**Файлы:** `index.html`, `sitemap.xml`.

- [x] В главной непосредственно после H2 «Типовые объекты, которые мы сканируем» и до сетки карточек добавить одну видимую ссылку. Она должна оставаться доступной на мобильном экране:

```html
<p style="margin:16px 0 28px;"><a href="/uslugi/obmery-pomeshcheniy/" style="font-family:'Manrope',sans-serif; font-weight:600; color:#9CC0FF; text-underline-offset:4px;">Обмеры квартир и помещений</a></p>
```

- [x] Перед закрывающим `</urlset>` в `sitemap.xml` добавить:

```xml
  <url>
    <loc>https://3d-scan-nn.ru/uslugi/obmery-pomeshcheniy/</loc>
  </url>
```

Ожидается два URL: главная и новая страница, без дублей и без `privacy.html`. Отправлять карту в кабинеты в рамках локальной реализации не требуется.

## 6. Проверить результат локально

- [x] Выполнить `node --test tests/calculator.test.cjs tests/contact-events.test.cjs tests/lead-form.test.cjs tests/interior-contact-events.test.cjs`: 20 исходных и четыре новые проверки. Новые проверки сначала воспроизвели отсутствие страницы, после реализации прошли; ожидания событий определяются каналом ссылки. Сопоставить diff главной с единственной добавленной ссылкой; обработчики и тарифы должны совпадать с исходной версией.
- [x] Разобрать новый HTML и XML локально: один H1, точные title/description/canonical, один URL новой страницы в sitemap, восемь содержательных блоков и семь вопросов. Сверить четыре тарифные строки и два примера с калькулятором.
- [x] Проверить ссылки и ресурсы: каждый локальный файл существует, `/#leadform` разрешается в секцию главной с `id="leadform"`, `#prices` — в цены новой страницы. Проверить прямое открытие вложенного URL, переход с главной, обратную ссылку и политику.
- [x] Для браузера использовать временный локальный сервер с CSP, блокирующей внешние соединения и отправку форм. Подменять Метрику только в выдаваемой тестовой копии, до выполнения её обработчиков. Файлы публикации тестовыми заглушками не менять. Прямые ссылки внешних каналов проверять без открытия мессенджеров и отправки сообщений.
- [x] Снять и осмотреть экраны 1440, 768, 390 и 360 px: отсутствие горизонтального переполнения, видимый минимум заказа, читаемые цены, доступные кнопки, читаемый FAQ и уведомление cookie. На узком экране пройти по кнопке к форме главной: тип объекта и результат остаются невыбранными, поля доступны.
- [x] С подменённым счётчиком проверить один клик по каждому контакту: ровно одно соответствующее событие; второй клик даёт ещё одно. Переходы к форме, ценам, политике и главной не дают `lead` или контактных событий. При отсутствующей либо выбрасывающей исключение `ym` навигация продолжает работать. Реальные заявки и события не отправлять.
- [x] Проверить работу FAQ и клавиатурный фокус. Устранить ошибки собственной страницы; ожидаемые сообщения CSP о блокировке внешнего счётчика отличать от ошибок приложения.
- [x] Остановить свой временный сервер и удалить только временные файлы текущей проверки. Выполнить `git diff --check`, просмотреть итоговый diff и убедиться в отсутствии посторонних изменений.
- [x] Обновить `PROJECT_STATUS.md` фактическими результатами: какие файлы созданы/изменены, какие проверки прошли, ограничения и статус публикации. Не объявлять страницу опубликованной или включённой в индекс по локальным проверкам.

## 7. Завершение в пределах разрешения

Локальный этап завершён, когда все проверки выше пройдены и результат показан владельцу. Подготовить краткий отчёт со ссылкой на локальную страницу и оставшимся решением о публикации.

Если поручена только локальная работа, на этом остановиться. При отдельном разрешении на завершение перепроверить действующую процедуру в `PROJECT.md`: отправка в `main` запускает GitHub Pages. Фиксировать только относящиеся к задаче файлы; после публикации отдельно проверить доступность и соответствие опубликованной версии. Новые обращения в кабинеты и запросы индексирования этим планом не поручаются.
