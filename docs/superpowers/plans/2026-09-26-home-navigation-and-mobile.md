# Улучшение главной — план локальной реализации

> Исполнение: последовательно через `executing-plans`, без параллельных агентов и нового worktree. Пользователь согласовал вариант A ответом «да» 26.09.2026. Commit, push и публикация в текущее поручение не входят.

**Цель:** понятный выбор услуги, честная подача демонстрационных примеров и удобное обращение с телефона.

**Архитектура:** существующий `index.html`, локальные стили и небольшой отдельный встроенный скрипт для меню и подписей `select`. Действующие скрипты расчёта, формы и аналитики сохраняются.

**Технологии:** HTML/CSS/vanilla JavaScript, существующие тесты `node:test`; без установки зависимостей.

**Спецификация:** [согласованный вариант A](../specs/2026-09-26-home-navigation-and-mobile-design.md).

## 1. Проверки нового поведения

Файл: `tests/home-navigation.test.cjs`.

- [x] Проверить настоящий встроенный скрипт через `node:vm`, подменяя только DOM.
- [x] Проверка: ссылка меню закрывает `details`, передаёт фокус целевому разделу и сохраняет обычный переход.
- [x] Проверка: Escape закрывает открытое меню и возвращает фокус на `summary`.
- [x] Проверка: полный текст выбранного варианта виден в связанной подписи, обновляется после `change`, не меняет `value` и сохраняет существующее `aria-describedby`.
- [x] Запустить `node --test tests/home-navigation.test.cjs`; до реализации ожидается падение из-за отсутствия нового скрипта.

## 2. Разметка и содержание

Файл: `index.html`.

- [x] Заменить навигацию на согласованные четыре ссылки; добавить нативное мобильное меню и доступные цели фокуса.
- [x] Установить утверждённые H1, пояснение, минимальную цену перед кнопками и основной переход `#leadform`; самостоятельный расчёт оставить по `#price`.
- [x] Перенести существующий блок объектов сразу после первого экрана и заменить его шестью компактными текстовыми карточками по таблице спецификации. Сохранить обычные `href` и ссылку для топографии/ВЛС/реверса.
- [x] У существующего блока результата добавить якорь `#deliverables`.
- [x] Переписать четыре демонстрационные карточки по согласованной таблице типовых задач. Убрать из блока фотографии, размеры и утверждения о выполненных заказах; файлы изображений не удалять.
- [x] Добавить подсказку перед существующей формой. Сохранить её поля, обработчик, endpoint и 12 контактных ссылок с прежними целями.

## 3. Мобильное поведение

Файл: `index.html`.

- [x] Добавить стили шапки, `details`, текстовых карточек, видимого фокуса и мобильного визуального блока.
- [x] До 560 px сделать только внутренние группы калькулятора одноколоночными, увеличить область нажатия вкладок до 44 px, показать подписи выбранных значений.
- [x] Добавить встроенный скрипт `home-navigation`: по ссылке закрывать меню и фокусировать раздел; по Escape закрывать меню и фокусировать `summary`; для каждого `#calc select` создать связанную текстовую подпись и обновлять её по `change`.
- [x] Запустить новые проверки, затем `node --test tests/*.test.cjs`.

## 4. Проверка результата

- [x] Сверить уникальность ID, один H1, существование локальных ссылок и якорей, отсутствие неподтверждённых размеров в `#cases`.
- [x] Сравнить скрипты расчёта, формы, Метрики и её подключения с исходным `HEAD`: их содержимое не меняется.
- [x] Осмотреть локальную страницу при 1440, 1100, 768, 390 и 360 px с исключённой Метрикой и CSP, блокирующей внешнюю отправку.
- [x] Проверить меню мышью/клавиатурой, переходы к помещению/форме, длинные варианты калькулятора и передачу расчёта. Форму не отправлять.
- [x] Закрыть временную вкладку, восстановить viewport, остановить локальный сервер.
- [x] Обновить `PROJECT_STATUS.md`, статус спецификации и этот план. Проверить ссылки документов и `git diff --check`, просмотреть весь diff.

Результат этапа — проверенные локальные изменения. Отправка в GitHub и установка на рабочий сайт выполняются по последующей команде владельца.

Проверено 26.09.2026: 27/27 тестов, пять ширин экрана, 35 локальных ссылок/ресурсов и якорей, 54 уникальных ID. Скрипты расчёта, формы и аналитики сохранены. При осмотре уточнён перенос строки согласия в мобильном CSS без изменения формы или её обработчика. Локальная реализация завершена; публикации не было.
