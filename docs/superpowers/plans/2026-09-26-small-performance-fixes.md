# План малых исправлений после проверки скорости

**Цель:** реализовать три пункта [согласованного решения](../specs/2026-09-26-small-performance-fixes-design.md).

**Архитектура:** существующая статика без зависимостей и изменения оформления. Выполнение последовательно в текущем репозитории, без агентов и новой рабочей копии, по `executing-plans`; правила проекта имеют приоритет.

**Файлы:** `index.html`, новый `favicon.ico`, `PROJECT_STATUS.md`, эта спецификация и план. Исходные изображения, шрифты и тесты не изменяются.

- [x] Проверить Git: чистая `main`, исходная запись `d0674cc`. Подтвердить WebP 720×720, PNG 64×64 и отсутствие `favicon.ico`.
- [x] Удалить две строки `preconnect`, добавить `width="720" height="720"` только к `.hero-photo`.
- [x] Создать ICO: заголовок ICONDIR, одна запись ICONDIRENTRY 64×64 / 32 bpp, затем неизменённые байты `favicon.png`. Проверить сигнатуру, размер и смещение вложенного PNG.
- [x] Запустить `node --test tests/*.test.cjs`: прошли 27 проверок. Новые тесты, повторяющие атрибуты HTML, не добавлялись.
- [x] На временном сервере 127.0.0.1 исключить Метрику только из проверочного ответа и заблокировать внешние запросы/отправку формы CSP. Сравнить текущую страницу с HTML из `HEAD` на 1440/390/360 px; проверить резервирование места при задержке фото, декодирование ICO и отсутствие переполнения. Сохранить и осмотреть снимки 1440/390 px.
- [x] Проверить, что все встроенные скрипты, HTML формы и исходные значки неизменны; проверить локальные ссылки и `git diff --check`.
- [x] Обновить только актуальный статус и отметки этого плана. Остановить временные процессы, удалить собственные временные файлы. Сообщить локальный результат без утверждения о публикации или новом балле PageSpeed.

Результат: фото резервирует 438×438 px до загрузки на 1440 px; после загрузки геометрия не изменилась. Снимки до/после на 1440/390 px совпадают. ICO декодируется как 64×64, локальный ответ 200. Запросов к Метрике/Formspree нет. Точная HTML-проверка подтверждает только согласованные три изменённые строки (две удалены, одна дополнена); новый ICO содержит исходный PNG без изменения пикселей. Временный стенд удалён. Публикация не выполнялась.

Спецификация и план сверены с согласованными тремя пунктами; новых требований и зависимостей нет.

После локальных проверок владелец поручил «закрой задачу». Перед фиксацией повторно прошли 27 тестов и проверка ICO/локальных ссылок; удалённая `main` подтверждена на `d0674cc`. Разрешены одна фиксация пяти файлов, обычная отправка `main` и штатная публикация по `PROJECT.md`. Фактический результат публикации и внешних проверок сохраняется в `PROJECT_STATUS.md`; предыдущий абзац описывает окончание локального этапа.
